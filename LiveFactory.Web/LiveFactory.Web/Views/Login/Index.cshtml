
@{
    ViewData["Title"] = "登录";
}


@section Css{
    <link href="https://cdn.bootcss.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.bootcss.com/jquery/2.1.1/jquery.min.js"></script>
    <script src="https://cdn.bootcss.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <!-- 引入样式 -->
    <link rel="stylesheet" href="https://unpkg.com/element-ui/lib/theme-chalk/index.css">

    <script src="https://cdn.bootcss.com/vue/2.2.2/vue.min.js"></script>
    <!-- 引入组件库 -->
    <script src="https://unpkg.com/element-ui/lib/index.js"></script>
    <style>
        button.btn {
            height: 50px;
            margin: 0;
            padding: 0 20px;
            vertical-align: middle;
            background: #de615e;
            border: 0;
            font-family: 'Roboto', sans-serif;
            font-size: 16px;
            font-weight: 300;
            line-height: 50px;
            color: #fff;
            -moz-border-radius: 4px;
            -webkit-border-radius: 4px;
            border-radius: 4px;
            text-shadow: none;
            -moz-box-shadow: none;
            -webkit-box-shadow: none;
            box-shadow: none;
            -o-transition: all .3s;
            -moz-transition: all .3s;
            -webkit-transition: all .3s;
            -ms-transition: all .3s;
            transition: all .3s;
        }

            button.btn:hover {
                opacity: 0.6;
                color: #fff;
            }

        header {
            background: none;
        }
    </style>
}



<!-- Top content -->
<div class="top-content">

    <div class="inner-bg">
        <div class="container" id="loginApp">
            <div class="row">
                <div class="col-sm-8 col-sm-offset-2 text">
                    @*<h1><strong>佳峰</strong> 欢迎您登录</h1>*@
                    @*<div class="description">
                            <p>
                                This is a free responsive login form made with Bootstrap.
                                Download it on <a href="http://azmind.com/"><strong>AZMIND</strong></a>, customize and use it as you like!
                            </p>
                        </div>*@
                </div>
            </div>
            <div class="row">
                <div class="col-sm-6 col-sm-offset-3 form-box">
                    <div class="form-top">
                        <div class="form-top-left" style="color:white">
                            <h3>欢迎您登录</h3>
                            <p>输入您的用户名和密码登录:</p>
                        </div>
                        <div class="form-top-right">
                            <i class="fa fa-lock"></i>
                        </div>
                    </div>
                    <div class="form-bottom">
                        <form id="LoginForm" method="post" class="login-form" action="@Url.Action("Login", "Login")">
                            <input type="hidden" id="returnUrl" name="returnUrl" value="@ViewBag.returnUrl" />
                            <div class="form-group">
                                <label class="sr-only" for="form-username">Username</label>
                                <input name="Account" placeholder="Username..." class="form-username form-control" id="form-username" type="text" required>
                            </div>
                            <div class="form-group">
                                <label class="sr-only" for="form-password">Password</label>
                                <input name="Password" placeholder="Password..." class="form-password form-control" id="form-password" type="password" required>
                            </div>
                            <button id="LoginButton" type="submit" class="btn" onclick="Login()">确定登录</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    @section scripts{
        <script src="https://cdn.bootcss.com/jquery-backstretch/2.0.4/jquery.backstretch.min.js"></script>
        <script>
                $(function () {
                    $(".container").css({ opacity: .9 });   //设置透明度
                    $.backstretch([
                        "/images/bg/bg1.jpg"
                    ], { duration: 10000, fade: 750 });
                });
                $("#LoginButton").keyup(function (e) {
                    if (e.keyCode == 13) {
                        Login();
                    }
                });
                function Login() {
                    var $loginForm = $('#LoginForm');
                    $loginForm.submit();
                    @*$loginForm.submit(function (e) {
                        //e.preventDefault();

                        //if (!$loginForm.valid()) {
                        //    return;
                        //}
                        $.ajax({
                            url:'@Url.Action("Login", "Login")',
                            data: { Account: $("#form-username").val(), Password: $("#form-password").val() },
                            success: function (result) {
                                //var json = $.parseJSON(result);
                                if (result.success) {
                                    window.location.href = "/Home/index?account=" + result.data["account"]
                                }
                            }
                        })
                        //    .then(function (resp) {
                        //    abp.ui.clearBusy('body');
                        //    if (resp.success) {
                        //        var url = $("#returnUrl").val();
                        //        if (url != "" && resp.url) {
                        //            window.location.href = url;
                        //        }
                        //        else {
                        //            window.location.href = "/";
                        //        }
                        //    }
                        //    else {
                        //        abp.message.error(resp.message);
                        //    }
                        //})
                    });*@

                    //$('#ReturnUrlHash').val(location.hash);
                    //$('#LoginForm input:first-child').focus();
                }
        </script>
    }
</div>

